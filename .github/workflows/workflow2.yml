name: Linter

on:
  workflow_call:
    secrets:
      PAT_TOKEN:
        required: true
jobs:
  linter:
    runs-on: ubuntu-latest
    environment: TestEnvironment
    steps:
      - name: Step 1
        run: |
          if [ "${{ secrets.PAT_TOKEN }}" == "123" ]; then
            echo "Match"
          else
            echo "Not matched"
          fi
