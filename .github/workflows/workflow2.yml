name: Linter

on:
  workflow_call:
  push:
    branches-ignore:
      - test

jobs:
  # if: github.event.pull_request != null
  linter:
    name: linter
    runs-on: ubuntu-latest
    steps:
      - name: Start linter
        env:
          ZZZ: ${{ 123 }}
        run: echo "Run Linter"
